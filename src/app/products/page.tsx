import { CharacterSelect } from "@/components/CharacterSelect";

export const metadata = {
  title: "Select Your Bag — HYDE",
};

export default function ProductsPage() {
  return <CharacterSelect />;
}
